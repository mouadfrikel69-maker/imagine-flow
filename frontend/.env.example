# Production API base URL for the FastAPI backend (used for /api/caption)
# Leave empty for local dev (Vite proxy forwards /api to VITE_API_PROXY)
VITE_API_URL=

# Optional: override the dev-server proxy target for /api/* requests
# VITE_API_PROXY=http://127.0.0.1:8000
